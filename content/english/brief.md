---
title: "Five minutes beats four feeds"
description: "A free daily digest of what actually moved in AI, DevOps, IT, Tech and Fintech. One email each weekday, written for people who ship."
layout: brief
url: /brief/
image: images/covers/2026-09-02-devops.jpg
sitemap:
  disable: true
---

<!--
The paid-traffic landing page. It has no body: everything on it is rendered by
layouts/_default/brief.html from the site's own published issues, so the proof
cannot go stale.

`layout: brief` is what selects that template, and the template is a complete
document rather than a block, so the site header -- whose nav links Crypto and
the Markets archive -- never renders here. That is an ad-policy requirement,
not a design preference; see the comment at the top of the template.

`sitemap: disable` keeps it out of sitemap.xml. Nothing on the site links here
and it exists to be arrived at from an ad, so indexing it would only put a
second, header-less version of the homepage into search results competing with
the real one.

`image` is the og:image for the ad's link preview. A JPEG, not the WebP: it is
crawlers that read the singular key and several of them drop WebP, which
unfurls the link with no picture at all.
-->
